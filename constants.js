// Explorer URL you use your local ord node explorer
// run with `ord server --http-port 8080`
// default is ordinal.com but you should use your own
const EXPLORER = 'https://ordinals.com';

const MODE = 'regtest';

module.exports = { EXPLORER, MODE };
